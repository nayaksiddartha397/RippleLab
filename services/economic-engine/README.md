# RippleLab economic engine

FastAPI service that will own deterministic calculations, validation, simulations, formula versions and evidence-linked result assembly.

```bash
uv sync --project services/economic-engine --dev
pnpm dev:api
```

The Day 1 service exposes `/` and `/health`. Financial calculators begin on Day 6.
